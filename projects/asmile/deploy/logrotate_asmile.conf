# Logrotate config for Asmile logs
# Install: sudo cp logrotate_asmile.conf /etc/logrotate.d/asmile

/var/log/asmile_*.log {
    weekly
    rotate 4
    compress
    delaycompress
    missingok
    notifempty
    create 644 asmile2 asmile2
}
